import { Copypaste } from './copyDirective';

describe('Copypaste', () => {
  it('should create an instance', () => {
    const directive = new Copypaste();
    expect(directive).toBeTruthy();
  });
});
