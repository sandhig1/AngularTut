import { Pastedirective } from './pastedirective';

describe('Pastedirective', () => {
  it('should create an instance', () => {
    const directive = new Pastedirective();
    expect(directive).toBeTruthy();
  });
});
